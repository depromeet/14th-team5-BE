spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        create_empty_composites:
          enabled: true
        show_sql: false
        format_sql: false
  flyway:
    enabled: true
    baseline-on-migrate: true
  h2:
    console:
      enabled: false
