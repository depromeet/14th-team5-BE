repositories {
    mavenCentral()
}

dependencies {
    implementation project(':common')
    implementation project(':member')
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}
