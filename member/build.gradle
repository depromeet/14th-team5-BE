repositories {
    mavenCentral()
}

dependencies {
    implementation project(':common')
    implementation project(':family')
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}
